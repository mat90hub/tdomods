# A Tcl module to write table into ODS file

This Tcl module create object of the class tdomdos which allow to write table into ODS file, readable and modifiable under LibreOffice Calc.
The table must be in the form of list lines.

See other module datatable, if you need to convert between different format of table before writing in the ODS file.

More help and description is included as comments in the file tdomods.tm itselft.
